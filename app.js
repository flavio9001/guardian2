const API_BASE = '/api'; let state = { user: null, people: [], groups: [], chats: [], schedule: [] }; const api = async (endpoint, method = 'GET', body = null) => { try { const options = { method, headers: { 'Content-Type': 'application/json', 'Authorization': `Bearer ${localStorage.getItem('token')}` } }; if (body) options.body = JSON.stringify(body); const res = await fetch(`${API_BASE}${endpoint}`, options); if (!res.ok) throw new Error('API Error'); return await res.json(); } catch (e) { console.error(e); throw e; } }; const login = async (credentials) => { const data = await api('/login', 'POST', credentials); localStorage.setItem('token', data.token); await loadState(); }; const loadState = async () => { state = await api('/state'); render(); }; const render = () => { const app = document.getElementById('app'); app.innerHTML = `<div>Dashboard: ${state.people.length} pessoas</div>`; }; const exportResumo = async (data) => { const text = `Resumo: ${data.name}`; if (navigator.share) { await navigator.share({ title: 'Resumo', text }); } else { const blob = new Blob([text], { type: 'text/plain' }); const url = URL.createObjectURL(blob); const a = document.createElement('a'); a.href = url; a.download = 'resumo.txt'; a.click(); } }; const showEmployeeModal = (person) => { const modal = document.createElement('div'); modal.className = 'modal'; modal.innerHTML = ` <div class='badge'> <img src='${person.photo}' alt='Foto'> <div class='info'> <h2>${person.name}</h2> <p>Cargo: ${person.role}</p> <p>Grupo: ${person.group}</p> <p>Tel: ${person.phone}</p> <p><a href='https://wa.me/${person.whatsapp.replace(/\D/g, '')}' target='_blank'>WhatsApp</a></p> <p>Superior: ${person.manager}</p> <p>Email: ${person.email}</p> <p>Endereço: ${person.address}</p> <p>Resumo: ${person.bio}</p> </div> </div> `; document.body.appendChild(modal); }; const sendMessage = async (chatId, message) => { await api('/chats/message', 'POST', { chatId, message }); }; const updateSchedule = async (scheduleData) => { await api('/schedules', 'PUT', scheduleData); }; document.addEventListener('DOMContentLoaded', loadState);